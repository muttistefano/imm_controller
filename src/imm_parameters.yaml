imm_controller:
  joints: {
    type: string_array,
    default_value: [],
    description: "Name of the joints to control",
  }
  command_joints: {
    type: string_array,
    default_value: [],
    description: "Name of the joints to control",
  }
  control_interface: {
    type: string_array,
    default_value: [],
    description: "Names of the control_interface to command",
  }
  state_interface: {
    type: string_array,
    default_value: [],
    description: "Names of the state_interface",
  }
  robot_chain_root: {
    type: string,
    default_value: "",
    description: "Name of the robot chain root",
  }
  robot_chain_tip: {
    type: string,
    default_value: "",
    description: "Name of the robot chain tip",
  }
  amr_base_link: {
    type: string,
    default_value: "",
    description: "Name of the mobile robot base link",
  }
  robot_description_topic: {
    type: string,
    default_value: "robot_description",
    description: "Name of the robot description topic",
  }
  cmd_vel_topic: {
    type: string,
    default_value: "robot_description",
    description: "Name of the cmd_vel topic",
  }
  only_robot: {
    type: bool,
    default_value: false,
    description: "Whether to control the arm only or the whole imm",
  }
  omni: {
    type: bool,
    default_value: false,
    description: "Whether the mobile robot is omnidirectional or not",
  }
